---
description: "Git workflow for this repository"
trigger: always_on
---

# Git Workflow

- Use `git add` commands instead of creating GitHub pull requests.
- Avoid making remote branch changes.
- If `git add` causes issues, create a local branch using `git checkout`.
