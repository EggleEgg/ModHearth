<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="clr-namespace:ModHearth.UI"
        x:Class="ModHearth.UI.ModUpdateLogWindow"
        Title="Mod Update Log"
        Width="980"
        Height="560"
        MinWidth="820"
        MinHeight="480">

  <Grid Margin="12" RowDefinitions="Auto,*">

    <DataGrid x:Name="logList" Grid.Row="1"
              CanUserReorderColumns="True" CanUserResizeColumns="True" CanUserSortColumns="True"
              IsReadOnly="True" GridLinesVisibility="None" LoadingRow="LogListLoadingRow">
      <DataGrid.Styles>
        <Style Selector="DataGridRow:selected /template/ Rectangle">
          <Setter Property="Fill" Value="Transparent" />
        </Style>
        <Style Selector="DataGridCell TextBlock">
          <Setter Property="Foreground" Value="{Binding RowBrush}" />
        </Style>
      </DataGrid.Styles>
      <DataGrid.ContextMenu>
        <ContextMenu Opened="ModContextMenuOpened">
          <MenuItem Header="Delete local mod" Tag="delete-mod" Click="ModContextDeleteMod" />
          <MenuItem Header="Unsubscribe from steam mod" Tag="unsubscribe-steam" IsVisible="False"
                    Click="ModContextUnsubscribeSteam" />
          <MenuItem Header="Redownload steam mod" Tag="redownload-steam" IsVisible="False"
                    Click="ModContextRedownloadSteam" />
          <Separator />
          <MenuItem Header="Open Folder" Tag="open" Click="ModContextOpenFolder" />
          <MenuItem Header="Open Steam Page" Tag="open-steam" IsVisible="False"
                    Click="ModContextOpenSteam" />
          <MenuItem Header="Copy Mod ID" Tag="copy-id" Click="ModContextCopyId" />
        </ContextMenu>
      </DataGrid.ContextMenu>
      <DataGrid.Columns>
        <DataGridTextColumn Header="Date" Binding="{Binding DateText}" Width="160" />
        <DataGridTextColumn Header="Mod" Binding="{Binding ModName}" Width="220" />
        <DataGridTextColumn Header="Type" Binding="{Binding SourceType}" Width="120" />
        <DataGridTextColumn Header="State" Binding="{Binding StateText}"
                            SortMemberPath="StateSortRank" Width="120" />
        <DataGridTextColumn Header="Active" Binding="{Binding ActiveText}" Width="80" />
        <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="*" />
      </DataGrid.Columns>
    </DataGrid>

  </Grid>
</Window>